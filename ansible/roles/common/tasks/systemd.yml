- name: Update {{COMPONENT}} SystemD file
  ansible.builtin.template:
    src: systemd.service
    dest: /etc/systemd/system/{{COMPONENT}}.service

- name: {{COMPONENT}} enabled and started
  systemd:
    name: "{{COMPONENT}}"
    state: restarted
    enabled: True