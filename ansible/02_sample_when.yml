- name: when conditon
  hosts: localhost
  vars:
    abc: ABC
  tasks:
    - name: printing sample1
      when: "abc var == ABC"
      debug:
        msg: "abc var - ABC"
    - name: printing sample2
      when: "abc var ABC"
      debug:
        msg: "abc var - abc"
