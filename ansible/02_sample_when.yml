- name: when conditon
  hosts: localhost
  vars:
    abc: ABC
  tasks:
    - name: printing sample1
      when: abc == "AB"
      debug:
        msg: "abc var = ABC"
    - name: printing sample2
      when: abc == "ABC"
      debug:
        msg: "abc var = ABC"
